namespace ASTRASystem.Enum
{
    public enum OrderStatus
    {
        Pending,
        Confirmed,
        Packed,
        Dispatched,
        InTransit,
        AtStore,
        Delivered,
        Returned,
        Cancelled
    }
}
